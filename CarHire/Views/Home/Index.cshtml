@model IEnumerable<CategoryHomeModel>

@{
    ViewData["Title"] = "Home Page";
}

@foreach (var cat in Model)
{
    <div class="text-center">
        <a class="display-7" asp-controller="Vehicles" asp-action="ByCategory" asp-route-categoryId="@cat.CategoryId">@cat.Name</a>
    </div>
}
